<div id="message-actions">
  <%= form_with(model: message, url: room_messages_path(@room), method: :post, id: 'message-form') do |form| %>

    <%= form.text_field :content %>
    <%= form.hidden_field :room_id, value: @room_id %>

    <div>
      <%= form.submit %>
    </div>
  <% end %>
</div>
